on:
  pull_request:
    branches:
      - master
    paths-ignore:
      - '**/*.md'
  workflow_dispatch:

name: Standard

jobs:
  deploy:
    name: Deploy to K8S
    if: ${{ success() && github.event_name != 'pull_request_target' && github.event_name != 'push' && !contains(github.event.head_commit.message, '[ci skip]')}}
    runs-on: ubuntu-latest
    steps:
      - name: Dump GitHub Event Name
        id: github_event_name
        run: echo '${{ github.event_name }}'
      - name: Checkout
        uses: actions/checkout@v3
        with:
          repository: GabSnow24/bot-wpp-config
          branch: master
      - name: Kustomize
        uses: imranismail/setup-kustomize@v1
        with:
          kustomize_version: v3.6.1
      - name: Update K8S
        run: |
          cd kube
          cd dev
          kustomize edit set image bot-wpp=qzzdocker24/bot-wpp:${{github.sha}}
      - name: Commit tag
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "Deploy Action"
          git commit -am "chore: change image tag\n[ci skip]"
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          force_with_lease: true
